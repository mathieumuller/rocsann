{% extends 'base.html.twig' %}

{% block body %}
    <div class="row criteria">
        <div class="col s12 m12">
            {{ form(criteria) }}
        </div>
    </div>
    <div class="row criteria">
        <div class="col s12 m12">
                <h2 class="header">Liste des créations</h2>
                <div class="row">
                    {% include 'creations/creation_item.inc.html.twig' with { creations: creations, rowItems: 3 } only %}
                </div>
                {#<div class="row hide-on-small-only hide-on-large-only">
                    {% include 'creations/creation_item.inc.html.twig' with { creations: creations, rowItems: 2 } only %}
                </div>
                <div class="row hide-on-medium-and-up">
                    {% include 'creations/creation_item.inc.html.twig' with { creations: creations, rowItems: 1 } only %}
                </div>#}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {% javascripts 'assets/app/js/creationsList.js' %}
        <script src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}

